
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - KASSAL_API_KEY=${KASSAL_API_KEY}

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "80:80"
